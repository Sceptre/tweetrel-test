version: '2.1'

orbs:
  twitter-orb: jakousa/twitter-orb@1.0.1

jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: jakousa/twurl:latest
    steps:
      - twitter-orb/tweet:
          access_secret: API_KEY
          access_token: API_SECRET
          consumer_key: ACCESS_TOKEN
          consumer_secret: TOKEN_SECRET
          contents: |
            CIRCLE_PROJECT_REPONAME has released CIRCLE_TAG 
            Check it out here: << pipeline.project.git_url >>

workflows:
  example-workflow:
    jobs:
      - example-job